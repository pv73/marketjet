import 'package:flutter/material.dart';

class More_Screen extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return SafeArea(
        child: Container(
          child: Center(
            child: Text("More_Screen"),
          ),
        ),
    );
  }
}
